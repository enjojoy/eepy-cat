// import { groth16 } from "snarkjs";

// let zkey = await fetch("/sleep_final.zkey").then(r => r.arrayBuffer());
// let wasm = await fetch("/sleep.wasm").then(r => r.arrayBuffer());

// export async function generateProof(gyroCount) {
//   const input = { gyroCount };

//   return await groth16.fullProve(input, wasm, zkey);
// }